<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:background="@drawable/bg_item_ingrediente"
    android:orientation="horizontal"
    android:padding="12dp"
    android:layout_marginBottom="12dp"
    android:gravity="center_vertical">

    <TextView
        android:id="@+id/tvIngrediente"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_weight="1"
        android:text="Ingrediente ejemplo"
        android:textColor="#44291D"
        android:textSize="16sp" />

    <ImageView
        android:id="@+id/btnEliminar"
        android:layout_width="24dp"
        android:layout_height="24dp"
        android:src="@drawable/eliminaringrediente"
        android:contentDescription="Eliminar ingrediente"
        android:padding="2dp" />
</LinearLayout>
